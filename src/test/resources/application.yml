chat:
  implementation: ${IMPL:local}
ollama:
  base-url: https://ollama.com
open:
  weather:
    key: key
    url:
      base-url: http://localhost:${wiremock.server.port}
    parameters:
      current: /weather
      forecast: /forecast
      city: Minsk
      mode: json
      units: metric
      lang: en
      cnt: 4
